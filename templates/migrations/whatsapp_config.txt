import BaseSchema from '@ioc:Adonis/Lucid/Schema'

export default class extends BaseSchema {
  protected tableName = '{{ tokensTableName }}'

  public async up() {
    this.schema.createTable(this.tableName, (table) => {
      table.increments('id').primary()
      table.string('webhook_route).notNullable().unique()
      table.string('timeout').notNullable()
      table.string('phone_number_id').notNullable().unique()
      table.string('whatsapp_business_id').notNullable().unique()
      table.string('access_token).notNullable()
      table.string('verify_token').notNullable()
      table.string('graph_url').nullable().defaultTo('https://graph.facebook.com')
      table.string('graph_version').nullable().defaultTo('v16.0)

      /**
       * Uses timestamptz for PostgreSQL and DATETIME2 for MSSQL
       */
      table.timestamp('expires_at', { useTz: true }).nullable()
      table.timestamp('created_at', { useTz: true }).notNullable()
    })
  }

  public async down() {
    this.schema.dropTable(this.tableName)
  }
}